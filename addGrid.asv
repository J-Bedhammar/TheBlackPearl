function [outimg] = addGrid(inimg)
%Adds a grid intop of the image to visualize the pearls position

%%
PearlsPerRow = 4;

[width,height] = size(inimg);

ratio = height/width;

PearlsPerCol = floor(PearlsPerRow*ratio);

ColDist = PearlsPerRow/width;
RowDist = PearlsPerCol/height;

outimg = inimg;

for w = 1: ColDist:width
     outimg(:,w,:) = 0;
end
    for h = 1:height
       
%%
outimg = inimg;
outimg(:,10:20,:) = 0;
end

