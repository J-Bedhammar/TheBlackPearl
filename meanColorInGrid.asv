function [meanGrid] = meanColorInGrid(im,ColDist,RowDist)

[x,y]=size(im(:,:,1));
meanGrid = zeros(x/RowDist,y/ColDist,3);
couunterx = 1;
countery = 1; 
lowerLimit = 1;
upperLimit = pearlSize;
for r = 1:RowDist:x
    for c = 1 : ColDist : y
        meangrid(counterx,countery,1) = mean(
    
    end
end

